#!/bin/sh
{{ if eq .container "archbox" }}
distrobox-host-exec sudo systemctl enable tailscaled --now
distrobox-host-exec sudo tailscale up --authkey={{- (rbw "tailscale" "key").notes }} --operator={{ .chezmoi.username }} --advertise-tags=tag:ssh-out
{{ end }}
